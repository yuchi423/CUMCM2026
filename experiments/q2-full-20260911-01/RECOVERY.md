# 本轮运行与恢复记录

数值求解按addbc5d提交运行，全部轨迹和参数选择完成。初次独立审计在写JSON时遇到NumPy布尔值不能直接序列化；此前检查已执行，但当时未生成最终审计文件，因此不以初次执行声称通过。

fcd738f把结果显式转为Python bool后，重新运行完整独立审计并成功，independent_audit.json为实际验收证据。failure.json保留初次编排失败原貌，现已由独立审计通过记录解除，不删除失败历史。

F盘不支持Node模块junction，本次在系统临时目录创建任务专用连接并复制同一个workbook.mjs运行。主入口现支持--node-modules自动处理这一环境差异；没有改系统包。报告图1因正式策略与固定0.8完全重合，合并同一曲线展示；所有数值求解输出未改动。

为使输入快照的SHA256在不同系统检出后仍可核验，.gitattributes为全年inputs.json关闭行尾自动转换。原始附件一直只读，模型输入哈希与实际参数见run.json。
