# T-009 第三问三路线实测交接

> 2026-09-12更新：用户已选择方向2为主模型。下文保留原比较阶段记录；当前选择及结果入口见 `memory/decisions/D-013-q3-main-confirmed.md` 与 `deliverables/q3/README.md`（仓库相对路径）。


- 时间：2026-09-11，Asia/Shanghai；分支：`codex/T-009-q3-model`。
- base_commit：`2e816ea68902cdd8e58f8e5c54ac86da9cb53f79`。
- 实验模型提交：`47027ac`；实际运行目录：`experiments/q3-models-20260911-04`。
- 目标：三条方向全部运行，必要结果进入Git待提交区，完整逐时细节仅本地保留。

## 已完成

控制组`no_update`、`state_only`，方向1`rolling_point`、方向2`rolling_margin`、方向3`rolling_scenario`及映射敏感性`rolling_point_step`均完成334天。汇总、分期、发布时刻调整、指定日期摘要、预报映射误差、场景选择、三张PNG/PDF图、报告、运行配置和审计摘要均在04目录。数值冠军方向3为1369.73万元；综合稳定性/复杂度暂推荐方向2为论文主线，依据D-012。

完整`details/`含六组逐10分钟执行、更新摘要和旧/新计划版本，用于本轮独立审计；按用户“只把必要结果放入待提交区”的要求被Git忽略。可由一键入口重建，不依赖绝对路径。独立审计通过315360条执行记录和7650次版本费重算。

## 失败与修正

- 01目录在求解前因附件3使用未补零日期停止；解析器已改为兼容原格式。
- 02目录在场景内部计划审计遇到1e-5kWh数值残差停止；为严格计划限定2e-5kWh容差。
- 03目录发现更实质的问题：低净负荷场景中“期末恰好1200”与光伏优先可能冲突。按选型定义改为“期末不低于1200”，提交47027ac后全量重跑04。02/03留本地并忽略，不进入待提交区。
- 后处理增加预报映射误差和模型推荐，不改变04模型数值；相关代码处于Git待提交区。

## 恢复与下一步

Python环境：3.12.14，NumPy 2.5.3，SciPy 1.18.1；另需openpyxl、Matplotlib。仓库根目录执行：

```text
python final_run/q3/main.py experiments/<新的运行ID>
```

输入哈希在04/run.json；配置为`configs/q3_models.json`，种子为空（无随机抽样）。接手先读D-012和04/model_report.md，核查线性插值口径；若接受方向2，再单独冻结参数选择及result3.xlsx生成任务。当前没有运行中进程。不要将04报告中的回测结论称为正式验收或外部盲测。

承载本记录的提交：`git log -1 --format=%H -- memory/handoffs/2026-09-11-T-009-q3-model.md`。当前按用户要求仅准备Git待提交区，尚未提交或推送结果阶段。
