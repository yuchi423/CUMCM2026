# T-010 问题4-2价格预测PoC交接

- 时间：2026-09-12，Asia/Shanghai；分支：`codex/T-010-q4-price-poc`。
- 用户已批准双层目标与回退，记录为 `memory/decisions/D-015-q4-target-approved.md`。
- 最新实验：`experiments/q4-price-poc-20260912-02`；正式结论入口：`planning/q4-poc-results.md`。

## 已完成

附件4结构与哈希通过检查。固定问题2 `combined` 后运行固定价控制组、6个因果预测候选、6小时错位负对照和oracle，共9方法×84天。开发期按照库存调整费用和0.5%简化规则选出 `mean7`；它在验证期优于固定价，在评估期高735.76元，触发科学目标失败条件和预声明的 `lag1` 回退。

独立审计复算108,864条10分钟记录，物理、费用、SOC、预测重建及756个未来扰动检查全部通过；同时充放电0次，求解失败0次。三张图均已人工查看，中文字体和坐标正常，PNG为320dpi并附PDF。

## 环境与恢复

模型使用本机 `D:\Users\python.exe`：Python 3.11.9、NumPy 2.4.2、SciPy 1.17.1、Matplotlib 3.10.8。附件4只读解析使用Codex附带Python及OpenPyXL 3.1.5，不修改本机环境。完整命令见 `final_run/q4_poc/README.md`。

逐时 `dispatch.csv.gz`、计划 `plans.jsonl.gz` 和价格输入快照保存在实验目录，可由一键入口重建。当前未生成334天连续结果或 `result4-2.xlsx`。

## 下一步

停在Human Model Gate。若用户选择 `RUN Detailed PoC`，固定运行 `lag1` 与附件1固定价控制组的334天连续SOC，补逐月/极端价格分析和正式Excel；不得用验证或评估阶段重新选窗口。若 `lag1` 算法失败，按批准口径回退附件1固定价。
