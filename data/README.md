# 数据

`raw/` 原样保存可入库的原始附件，不覆盖；`processed/` 保存脚本生成的数据。先填写 MANIFEST.csv，记录仓库相对路径或队友可访问的外部位置、SHA256、来源、用途与获取方式。

SHA256：PowerShell 使用 `Get-FileHash -Algorithm SHA256 <文件>`；Linux 使用 `sha256sum <文件>`。字段含义、单位、缺失编码和清洗规则另写在对应数据说明中。外部地址不要带访问令牌。大文件先确定共同存储方式再上传，禁止静默忽略后宣称可复现。
