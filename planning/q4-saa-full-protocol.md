# 问题4两阶段SAA 334天正式运行协议

> 2026-09-13复核发现本协议对应的SAA场景层未显式保证光伏优先，旧运行不再作为最终结果；修复与重跑口径见 [q4-saa-strict-repair-protocol.md](q4-saa-strict-repair-protocol.md)。

日期：2026-09-12。用户已通过HUMAN MODEL GATE，批准冻结当前方案并运行334天。本协议在正式结果产生前固定。

## 冻结模型

主候选仅为 `saa_load`：目标日前14个已结束日构成供需预测残差场景；所有场景共享每个10分钟时段的普通购电量；名义场景沿用问题二 `combined` 的同星期负载预测、7日光伏预测和0.8分位余量，日末目标储电量1200kWh。名义层使用0--1充放电状态，历史场景采用连续补救变量和极小循环成本。实际结算使用问题二的因果逐时执行器。

模型参数、场景数、余量分位数、储能效率、容量、功率、5倍紧急购电价格和期末库存价值系数全部冻结，不根据334天结果调节。

## 比较对象

- 主候选：`saa_load`；
- 操作性回退：`mean7`；
- 其他基线：`mean14`、`fixed`；
- 非因果下界：`oracle_actual`，不参与选择。

联合价格—供需场景和错配负对照已经在Cheap PoC完成机制筛选，不进入全年正式选择。

## 运行范围与裁决

连续运行2025-02-01至2025-12-31，共334天。五种方法从同一初始储电量开始，各自在全年连续推进储能状态。

`saa_load`成为正式主模型须同时满足：

1. 能量平衡、储能状态、上下界、功率、互斥、费用复算和未来信息检查全部通过；
2. 全年库存调整费用相对 `mean7` 和 `fixed` 均至少降低0.1%；
3. 全年现金总支出不高于 `mean7` 与 `fixed` 中较低者；
4. 任一自然季度的库存调整费用不得比该季度较优基线高0.1%以上；
5. 紧急购电与已付未用电不得同时高于 `mean7`。

若任一费用或季度条件失败，执行预先声明的 `mean7` 操作性回退；不得事后引入季度参数或改变历史窗口。输出全年、季度、月份、逐日和10分钟账本，独立审计、PNG/PDF图和一键入口。
