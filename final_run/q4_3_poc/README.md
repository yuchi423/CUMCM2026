# 问题4-3 Cheap PoC入口

在仓库根目录运行：

```text
python final_run/q4_3_poc/main.py experiments/q4-3-poc-<唯一运行ID>
```

入口依次运行冻结的84天六策略比较、独立审计和Model Judge报告。输出目录必须不存在；完整10分钟账本保留在本地 `details/`，汇总、配置、审计和报告进入任务分支。
